import 'package:flutter/material.dart';

// Açık Tema Renkleri
class LightColors {
  static const primary = Color(0xFF6366F1); // Indigo-500
  static const secondary = Color(0xFF22D3EE); // Turkuaz-400
  static const surface = Color(0xFFFFFFFF); // Beyaz
  static const background = Color(0xFFF3F4F6); // Gri-100
  static const onPrimary = Color(0xFFFFFFFF); // Beyaz
  static const onSecondary = Color(0xFF181A20); // Koyu
  static const onSurface = Color(0xFF181A20); // Koyu
  static const onBackground = Color.fromARGB(255, 44, 48, 61); // Koyu
  static const error = Color(0xFFEF4444); // Kırmızı-500
  static const onError = Color(0xFFFFFFFF);
}

// Koyu Tema Renkleri
class DarkColors {
  static const primary = Color(0xFF7C3AED); // Mor-600
  static const secondary = Color(0xFF10B981); // Yeşil-400
  static const surface = Color(0xFF181A20); // Çok koyu mavi-gri
  static const background = Color.fromARGB(255, 50, 56, 58); 
  static const onPrimary = Color(0xFFFFFFFF); // Beyaz
  static const onSecondary = Color(0xFFFFFFFF); // Beyaz
  static const onSurface = Color(0xFFF3F4F6); // Açık gri
  static const onBackground = Color(0xFFF3F4F6); // Açık gri
  static const error = Color(0xFFEF4444); // Kırmızı-500
  static const onError = Color(0xFFFFFFFF);
}

// Genel Renkler
class AppColors {
  static const white = Color(0xFFFFFFFF);
  // Kategori renkleri
  static const website = Color(0xFF3B82F6); // Mavi
  static const banka = Color(0xFF10B981); // Yeşil
  static const veritabani = Color(0xFFF59E42); // Turuncu
  static const lisans = Color(0xFF8B5CF6); // Mor
  static const diger = Color(0xFF9CA3AF); // Gri

  static const black = Color(0xFF000000);
  static const grey = Color(0xFF9CA3AF); // Modern gri
  static const lightGrey = Color(0xFFF3F4F6); // Modern açık gri
  static const darkGrey = Color(0xFF374151); // Modern koyu gri
  static const transparent = Colors.transparent;
}
